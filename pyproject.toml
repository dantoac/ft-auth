[project]
name = "fasthtml-auth"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "bcrypt>=4.3.0",
    "python-fasthtml>=0.12.25",
    "ruff>=0.12.12",
]
